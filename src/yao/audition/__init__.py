"""A/B Audition UI — browser-based side-by-side comparison.

Provides a local FastAPI server for comparing two iterations of a
composition. Users can play sections of each iteration and record
preferences, which flow to the Reflection Layer.

Optional dependency: ``pip install yao[annotate]`` (FastAPI + uvicorn).
"""
