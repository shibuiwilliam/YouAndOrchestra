"""Neural generator bridges — optional backends for texture/melody generation.

All ``torch`` / ``audiocraft`` / ``transformers`` imports are confined
to this package. Missing dependencies produce clear error messages
with installation instructions.

Install neural backends: ``pip install yao[neural]``
"""

from __future__ import annotations
